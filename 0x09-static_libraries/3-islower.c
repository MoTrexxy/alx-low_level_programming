#include "main.h"

/**
* _islower - checks if a character is lowercase
* @c: the character to check
* Return: if c is lowercase return 1, else return 0
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
